{
  "name": "prj_sr_afro_Server",
  "version": "1.0.0",

  "main": "index.js",
  "scripts": {
    "start": "(pm2 delete %npm_package_config_processName% || echo) && pm2 save --force && pm2 start C:\\inetpub\\wwwroot\\%npm_package_config_projectPath%\\main.js --name %npm_package_config_processName% && pm2 save --force && pm2 logs",
    "restart": "pm2 start %npm_package_config_processName% && pm2 save --force",
    "delete": "(pm2 delete %npm_package_config_processName% || echo) && pm2 save"
  },
  "config": {
    "processName": "prj_sr_afro",
    "projectPath": "prj_sr_afro_gate_entry\\prj_sr_afro_Server\\"
  },
  "homepage": ".",
  "author": ""
}
